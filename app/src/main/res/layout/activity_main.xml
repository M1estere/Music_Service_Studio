<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="playerViewModel"
            type="com.example.music_service.MusicPlayerViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#020017">

        <FrameLayout
            android:id="@+id/frame_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@id/mini_player"
            android:orientation="vertical">

        </FrameLayout>

        <androidx.cardview.widget.CardView
            android:id="@+id/mini_player"
            android:onClick="@{()-> playerViewModel.backToPlayer()}"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_above="@id/nav_bar"
            android:backgroundTint="@color/black"
            android:elevation="0dp"
            android:visibility="visible">

            <ProgressBar
                android:id="@+id/track_progress"
                style="@style/Widget.AppCompat.ProgressBar.Horizontal"
                android:layout_width="match_parent"
                android:layout_height="4dp"
                android:background="@color/white"
                android:max="@{playerViewModel.maxProgress}"
                android:progress="@{playerViewModel.progress}" />

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginStart="10dp"
                android:layout_marginTop="5dp"
                android:layout_marginEnd="10dp"
                android:layout_marginBottom="5dp"
                android:background="@color/black">

                <ImageView
                    android:id="@+id/track_cover"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:layout_centerVertical="true"
                    android:src="@drawable/ic_launcher_background" />

                <LinearLayout
                    android:id="@+id/info_region"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_centerVertical="true"
                    android:layout_marginStart="10dp"
                    android:layout_toEndOf="@id/track_cover"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/title_txt"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@{playerViewModel.trackName}"
                        android:textColor="@color/white"
                        android:textSize="18sp" />

                    <TextView
                        android:id="@+id/author_txt"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@{playerViewModel.authorName}"
                        android:textColor="@color/grey"
                        android:textSize="15sp" />

                </LinearLayout>

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignParentEnd="true"
                    android:layout_centerVertical="true"
                    android:layout_marginStart="10dp"
                    android:layout_marginEnd="10dp"
                    android:orientation="horizontal">

                    <Button
                        android:id="@+id/previous"
                        android:layout_width="29dp"
                        android:layout_height="35dp"
                        android:layout_marginEnd="5dp"
                        android:backgroundTint="@color/white"
                        android:onClick="@{()-> playerViewModel.previousSong()}" />

                    <Button
                        android:id="@+id/pause"
                        android:layout_width="42dp"
                        android:layout_height="50dp"
                        android:backgroundTint="@color/white"
                        android:onClick="@{()-> playerViewModel.changePlayingState()}" />

                    <Button
                        android:id="@+id/next"
                        android:layout_width="28dp"
                        android:layout_height="35dp"
                        android:layout_marginStart="5dp"
                        android:backgroundTint="@color/white"
                        android:onClick="@{()-> playerViewModel.nextSong()}" />

                </LinearLayout>

            </RelativeLayout>

        </androidx.cardview.widget.CardView>

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/nav_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:background="@color/purple_200"
            app:itemTextColor="@color/grey"
            app:menu="@menu/bottom_nav_menu"/>

    </RelativeLayout>

</layout>